# Project toolchain, managed by mise — https://mise.jdx.dev
#
#   Setup:  mise install                     # installs the versions pinned below
#   Use:    mise exec -- node -v / pnpm -v    # or activate mise in your shell
#
# NOTE: Docker Engine is a system daemon and is NOT managed by mise.
#       Install it separately — see scripts/setup-host.sh and spec/local-dev.md.

[tools]
node = "22"     # backend runtime + frontend build (Node.js 22 LTS)
pnpm = "10"     # package manager for the api/ + web/ workspaces

# Deploy-time only (spec/architecture.md §10) — uncomment when provisioning GCP:
# terraform = "1.9"
# gcloud = "latest"
